(define-module (awb99 install chat)
  #:export (my-chat))


(define my-chat
  (list 
   "telegram-desktop"
   ;; IRC
   ; irc.libera.chat on port 6697.
   ; "weechat" ; IRC
   "quassel"    ; GUI replication of WeeChat.
   ; "hexchat" ;
   ))
