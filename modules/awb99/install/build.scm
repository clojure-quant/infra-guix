(define-module (awb99 install build)
  #:export (my-build))

(define my-build
  (list 
    "coreutils" ; glibcc gcc bash 
       ;"direnv" google this
      
        ;"idutils"
        ; "guile" 
        "hello"
        ;"nss-certs" ; tls certificates
       
        "make"
        "gcc-toolchain"

        ; "llvm" "clang" "clang-runtime" "clang-toolchain"
        ; "glibc" "gdb" "gcc-toolchain"
        ; "gnu-make" "cmake" "pkg-config"
        ; "patchelf" "binutils" "elfutils"



  ))

  